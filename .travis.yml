language: bash

env:
  - SHUNIT_COLOR='always'

script:
  - make build
  - make lint
  - make test-unit
  - make test-integration

addons:
  homebrew:
    packages:
      - jq
      - shellcheck
  apt:
    packages:
      - jq
      - shellcheck
#      - ksh
#      - zsh

matrix:
  include:
    - os: osx
    - os: linux
      dist: trusty
    - os: linux
      dist: xenial
